

Приоритетные задачи
Добавить handler в боте для вызова агента (/ask, /chat или просто любое сообщение вне FSM)

Исправить IndexError в парсинге reps в workout.py

Убрать test.db из репо и добавить в .gitignore

Защитить open(prompt_path) в агенте через try/except

Вынести get_llm() и build_trainer_graph() как синглтоны

Реализовать scheduler для утренних советов/напоминаний (папка пустая)